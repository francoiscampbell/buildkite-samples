# this pipeline simulates an app that has lint and parallel test runs
steps:
  - command: echo Running checks
    label: Checks
  - command: echo Running seed
    label: Seed
  - command: 08-small-app/tests.sh
    label: Tests
    parallelism: 8  
  - wait
  - command: echo Creating release
    label: Create release